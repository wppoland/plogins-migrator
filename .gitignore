/vendor/
/node_modules/
/build/
.DS_Store
.phpunit.result.cache
# Local-only wp-env overrides (mounts ../migrator-pro + sets a dev license flag).
.wp-env.override.json
# composer.lock IS committed — pins the dev toolchain (phpcs/phpstan/stubs).
